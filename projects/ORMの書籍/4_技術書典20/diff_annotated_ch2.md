# diff_annotated_ch2.md
AI清書（before）: `2026.3.13_ORM前史本_第二章_AI清書.md`
人力清書（after）: `2026.3.15_ORM前史本_第二章_人力清書.md`

対象: 第二章全体

---

### 変更 001
**種類:** REWRITE（文体統一）
**before:** `SQL文はソースコードに静的に固定され、実行時に内容を変えることはできない。またプレコンパイルの工程は特定のデータベース製品と強く結びついており、Oracle向けに書いたコードはDB2ではそのまま動かない。`
**after:** `SQL文はソースコードに静的に固定され、実行時に内容を変えることはできません。またプレコンパイルの工程は特定のデータベース製品と強く結びついており、Oracle向けに書いたコードはDB2ではそのまま動かせません。`
**意図:** 文体統一（常体→敬体）。

---

### 変更 002
**種類:** REWRITE（節タイトル変更）
**before:** `#### IBMの時代からPCの時代へ`
**after:** `#### IBMの時代からMicrosoftの時代へ`
**意図:** Embedded SQL → ODBCという技術の変遷はIBMの時代からMicrosoftの時代への移行と対応している。タイトルでその軸を明示することで、本章の主題を強調した。

---

### 変更 003
**種類:** DELETE（節の統合）
**before:** `#### GUIとクライアントサーバーの時代`（`IBMの時代からPCの時代へ` の次に独立した節）
**after:** 見出しが削除され、内容は `#### IBMの時代からMicrosoftの時代へ` の末尾に統合
**意図:** クライアントサーバーの内容量が薄く、独立した節を立てる強度がなかったため前節に統合。

---

### 変更 004
**種類:** REWRITE（Apple IIのMicrosoft言及の移動と再構成）
**before:**
```
...このApple IIにBASIC言語を提供したのが、まだ小さなソフトウェア会社だったMicrosoftです。

遅れて1981年、IBMもPC市場に参入します。このときIBMはPC向けのOS開発をMicrosoftに発注しました。
```
**after:**
```
...（Apple IIのVisiCalc説明のみで段落を閉じる）

遅れて1981年、IBMもPC市場に参入します。このときIBMはPC向けのOS開発を、当時まだ小さなソフトウェア会社ながらApple IIにBASIC言語を提供していたMicrosoftに発注しました。
```
（Microsoft紹介をIBM PC参入の文脈でまとめた）
**意図:** Apple・IBM・Microsoftと複数の会社が入り乱れてわかりにくい。「PCの登場」を扱う段落と「Microsoftが主役になる」段落を分けることで、読み手が各段落で追うべき会社を絞れるようにした。

---

### 変更 005
**種類:** ADD（1992年の明暗パラグラフの追加）
**before:** （OS/2とWindowsの話の後、すぐクライアントサーバーの話へ）
**after:**
```
1992年というのはMicrosoftとIBMにとって特別な年でした。WindowsはコンピュータのOSとして史上初の４ヶ月連続100万本の売り上げを達成し、創業者であるビル・ゲイツはパーソナルコンピュータ産業への貢献によりアメリカ国家技術賞を受賞しています。一方のIBMはその長い企業の歴史の中で初めての赤字転落となり、当時の米国企業史上最大の**49億6500万ドル**という損失を計上しています。
```
**意図:** IBMからMicrosoftへの世代交代を明確にするため。具体的な数字で両者の明暗を示すことで、時代の転換が読み手に伝わりやすくなる。

---

### 変更 006
**種類:** ADD（CLI節に図挿入メモの追加）
**before:** 即座にSAGの説明
**after:**
```
ここにSQLとSAGの関係性の図を入れる。SVGのやつ。
```
（本文の前にプレースホルダーとして挿入）
**意図:** SAG→X/Open→ISOという標準化の連鎖と、ODBCがCLI仕様の実装であるという関係を図で示すため。文字だけでは複数の標準化団体とバージョンの関係が把握しにくい。（対応図: `SQLとSAGの関係性.svg`）

---

### 変更 007
**種類:** REWRITE（MicrosoftのODBC実装節の再構成）
**before:**
```
MicrosoftがODBCをWindows専用として出した背景には、ある推測が成り立ちます。ODBCが普及するということは、どのデータベース製品を使っていてもWindows上で動くアプリケーションから一貫してアクセスできる環境が整うということです。つまりODBCの普及は、Windowsというプラットフォームの価値を高める。データベースそのものを持たないMicrosoftにとって、特定のDB製品への依存を薄める標準の推進は...
ODBCはCLI仕様をそのまま実装するのではなく、独自に拡張した三層構造を採用しました。
- コア層：SAGとISOのCLIに準拠した最小限の機能。各社が合意できた最小公倍数です。
- レベル1：...実務に必要な機能が加わります。
- レベル2：...
レベル1・2はMicrosoftが実務の要求に応えるために独自に定義した拡張です。
```
**after:**
```
ODBCとはOpen Database Connectivityの略です。名前の通り「データベース接続をオープンに」という思想が名称に込められています。
ODBCはCLI仕様をそのまま実装するのではなく、独自に拡張した三層構造を採用しました。
- コア層：SAGとISOのCLIに準拠した最小限の機能
- レベル1：...実務に必要な機能が加わった
- レベル2：...
レベル1・2はMicrosoftが実務の要求に応えるために独自に定義した拡張です。また、それ以上に特徴的な拡張として、ODBCでは透過的アクセスを実現していました。...（透過性の説明を先に）
これは当時の「部署ごとに異なるDBが導入され」...
データベースそのものを持たないMicrosoftにとって...（ジレンマなしの説明が後に移動）
```
**意図:** 「ある推測が成り立ちます」というフレーミングが著者の感想っぽく見えたため削除し、シンプルに書き直した。透過的アクセスの説明を三層構造の直後に置いたのは、「ODBCの透過性→Windowsプラットフォームの価値向上→MicrosoftにとってODBC推進の動機」という論理の根拠として使えるため。ジレンマなしの説明はその後に置くことで、推測でなく根拠ある説明として読める順序になる。

---

### 変更 008
**種類:** ADD（第二節のイントロ追加）
**before:** `### 第二節：ODBCの仕様と使い方` の直後に即、57関数の説明
**after:**
```
### 第二節：ODBCの仕様と使い方
ここからは、ODBCの仕様と、実際の使い方をみていきたいと思います。以下で紹介するのは1995年にリリースされたODBC3.0の実装です。

#### 仕様
```
（節を `#### 仕様` と `#### 実際の使い方` に分割）
**意図:** ソフトウェアはバージョンによって仕様が変わるため、いつの時点の情報かを明記することが必要。

---

### 変更 009
**種類:** REWRITE（仕様一覧をカテゴリ別リストに再構成）
**before:** `CLIの仕様書が規定していたのは、大きく分けると次のようなカテゴリの呼び出しでした。ハンドルの割り当てと解放、属性の取得と設定、...合計で57の呼び出しが仕様として定義されていました。`
**after:**
```
CLIの仕様書が規定していたのは、次のような57カテゴリの呼び出しでした。

  - 接続管理（10関数）
	  - ハンドルの割り当てと解放（8）
	  - 接続のオープンとクローズ（2）
  - 動作設定（16関数）
	  - 属性の取得と設定（10）
	  - デスクリプタへのアクセス（6）
  - SQLの実行と結果取得（17関数）...
  - 探索・制御・監視（14関数）...
```
**意図:** 10以上のカテゴリを散文で並べると理解が難しい。散文で伝えられるのは基本的に2つまで（深追いしない場合でも4つ程度が限界）。文章はシリアライズされた形式であり、構造的な情報の伝達には向かないため、この規模の列挙はリスト化して構造を見せる必要があった。

---

### 変更 010
**種類:** ADD（ハンドルのサブセクション追加）
**before:** ハンドルの説明がコードブロックの前文として一段落で説明
**after:**
```
##### ハンドル
まずはバンドルです。
...
- 環境バンドル
- 接続バンドル
- 文バンドル
```
（サブセクションとして独立、コード後の説明も追加）
**意図:** ハンドルが理解しにくいため、サブセクションを立てて解説を追加した。なお「バンドル」は誤字で正しくは「ハンドル」（handle）。人力清書ファイル側で修正が必要。

---

### 変更 011
**種類:** ADD（ディスクリプタ等のサブセクション追加）
**before:** （なし）
**after:**
```
##### ディスクリプタ・スキーマメタデータ・イントロスペクション
この三つはどれもメタデータの取得に関わるものになります。
- ディスクリプタ：今処理中のSQL文のパラメータ・結果列の形式
- スキーマメタデータ：DBサーバーの構造（カタログ情報）
- イントロスペクション：CLIの実装が何をサポートしているか
```
**意図:** 元の散文がわかりにくかったため、用語解説として整理。技術用語はそのまま残しつつ、一言で何を指すかをリストで示した。

---

### 変更 012
**種類:** ADD（実際の使い方セクションと新コードサンプル追加）
**before:** （なし）
**after:**
```
#### 実際の使い方
...DoStatement のswitch-caseコード...
（SELECT/DML/DDLの処理フローを示すコードサンプルと解説）
上記のような手続的な処理を当時は行っていました。現代のORMであれば、`user.all()`だけで全てを実行可能です。
```
**意図:** 読者が実際には使わない技術の解説なので、具体的な実例がないと理解が難しい。コードで実際の使い方を示すことで腑に落ちやすくした。末尾の `user.all()` との対比は、当時の煩雑さが伝わって面白いと感じたため追加。

---

### 変更 013
**種類:** REWRITE（節タイトルと内容変更）
**before:** `#### COBOL・C++対応`（COBOLを先に説明し、C++を補足）
**after:** `#### C++対応`（COBOLを簡潔に言及しつつ、MFCのODBCラッパーを具体的に説明）
**意図:** 書くうちに焦点が変わった。COBOLはメインではなく、「90年代にどういう使い方で覇権をとったか」という問いに答える方が重要。MFC標準組み込みという具体的な事実が、ODBCがWindows開発エコシステムに浸透した経緯を示す。

---

### 変更 014
**種類:** REWRITE（まとめのタイトル変更）
**before:** `### 第二章のまとめ：接続の自由を得た時代`
**after:** `### 第二章のまとめ`
**意図:** 比喩的なサブタイトルは意味がとりにくく認知コストだけが上がるため削除。見出しはシンプルに事実を示す（変更029と同じ方針）。

---

### 変更 015
**種類:** ADD（まとめ末尾へのJava言及追加）
**before:** `その歴史を見ていきます。`（でまとめが終わる）
**after:**
```
...その歴史を見ていきます。
ですが、OOPを世界に広めたJava言語が登場するのは1995年のこと。OOPの世界は目前に迫っていました。
```
**意図:** まとめの終わりが急だったため、次巻への引きとして追加。普遍的な編集ルールではなく、この箇所の読後感に対する感覚的な判断。
