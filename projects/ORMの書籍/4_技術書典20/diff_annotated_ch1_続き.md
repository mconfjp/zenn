# diff_annotated_ch1_続き.md
AI清書（before）: `2026.3.13_ORM前史本_第一章_AI清書.md`
人力清書（after）: `2026.3.15_ORM前史本_第一章_人力清書.md`

対象: 第一章 1-1-2（データバンク構想とデータベースの誕生）以降
※ 変更001〜019 は `diff_annotated.md` に記録済み。本ファイルは変更020から継続。
※ 見出しレベル変更（変更012〜019）は既記録のため、以降は内容変更のみ対象。

---

### 変更 020
**種類:** DELETE
**before:**
```
#### 1960年代：データバンク構想の誕生

プログラマブルなコンピュータが普及し始めた1960年代に入ると、データの管理に関する考え方に大きな転換が訪れます。それが「データバンク」という構想です。

当時のシステムでは、各部門や各アプリケーションがそれぞれ独自のファイルにデータを持つのが一般的でした。しかし業務が複雑化するにつれ、同じデータが複数の場所に重複して存在したり、部門間でデータの整合性が取れなくなったりといった問題が顕在化してきます。データバンク構想は、こうした課題に対する回答でした。システムの各所で記録されるデータを一箇所に集約して管理する。この発想は、現代のデータベースにつながる考え方の原点といえます。
```
**after:** （なし）
**意図:** 歴史的事実の誤り。「一箇所に集約して管理する」という発想はマスターファイルなど遥か以前から存在しており、1960年代のデータバンク構想に固有のものではなかったため削除。

---

### 変更 021
**種類:** REWRITE（節タイトル＋内容の入れ替え）
**before:** （1-1-2節の内容はメモ箇条書きのみ：diff_annotated.md 変更011）
**after:**
```
### 1-1-2. データベースの誕生
[[2026.3.20_DB誕生の整理]]
ここからはデータベースの歴史について考えてみましょう。
ホレリスの時代、データはパンチカードで保存していました。このパンチカードというものについて改めて考えてみると、これは記録媒体でありながら入出力のための媒体でもありました。カード一枚一枚がデータを保存するレコードでありながら、穴を開けることで入力を表現したり、データを読み取ることもできたというわけです。
パンチカードは当時の大量データを扱うという課題を解決しましたが、一方でいくつかの課題も持っていました。それは例えばカードがバラバラになると並び替えるのが大変であることや、単純に嵩張ってしまうことや、一度入力したカードはデータの書き換えが難しいことなどです。これらの特性は現代の記録媒体にはありません。なぜパンチカードにこれらの特性があるのかというと、それは入出力媒体としての利便性をとると、そうならざるを得ないから、という見方もできるでしょう。つまり、パンチカードはふたつの概念が混ざっているーーというより、ふたつの概念が未分化だった時代の記録媒体だった、とも言えるのではないでしょうか。
これを解決するため、いくつかの新しい記録媒体が登場しました。それは紙テープであったり、磁気ドラム、磁気テープ、磁気ディスクなどです。そしてそれら記録媒体が変化すると、その記録媒体に書き込む・読み取るためのシステムが必要となります。
```
**意図:** 変更020（データバンク構想の削除）によってパンチカードからDBMSまでの流れが途切れたため、その間を繋ぐ段落を新設。マスターファイル的発想は昔からあったので「データの集約」は新しくない。代わりに「記録媒体の変化」→「その媒体を管理するDBMSの登場」という軸で繋いだ。パンチカードが記録媒体と入出力媒体として未分化だったという視点も盛り込んだ。

---

### 変更 022
**種類:** REWRITE（タイトル変更）
**before:** `#### ファイルアクセスの時代：BDAM、ISAM、VSAM`
**after:** `#### ファイルアクセス：BDAM、ISAM、VSAM`
**意図:** 「の時代」が冗長なため削除。

---

### 変更 023
**種類:** DELETE
**before:**
```
興味深いのは、後年においてVSAMから直接Embedded SQLへ移行することを想定した書籍が存在したという事実です。IMSのような階層型データベースを介さず、ファイルアクセス層から直接RDB時代へ接続しようというニーズが現場にあったことを物語っています。

これらのファイルアクセス方式は、後に登場するIMSのストレージ層としても活用されました。データベース管理システムが登場した後も、その土台にはファイルアクセス方式の技術が息づいていたのです。
```
**after:**
```
これらのファイルアクセス方式は後続するデータベース管理システムのストレージ層としても活用されました。
```
**意図:** ネタとしては面白いが、直前の流れを邪魔するため削除。面白い情報でも流れを崩さない形でないと入れない方針。

---

### 変更 024
**種類:** DELETE + ADD（階層型・ネットワーク型DBの冒頭段落の入れ替え）
**before:**
```
データバンク構想が現実のシステムとして具体化していく中で、データ同士の「関係」をより構造的に扱うための仕組みが求められるようになりました。こうして登場したのが、階層型データベースとネットワーク型データベースです。

IBMはアポロ計画の部品管理という具体的な課題を解くために、1966年から開発を始めたIMS（Information Management System）を1968年に正式稼働させます。
```
**after:**
```
ファイルアクセス方式が確立されたのと同時期の1963年に、GeneralErectrics社のチャールズ・w・バッハマンによりIDS（Integrated Data Store）が発明されます。これはネットワーク型データベースであり、史上初めてのDBMSでした。ネットワーク型DBではナビゲーションという概念を持っており、エンティティ同士の関係性を定義したネットワーク上を物理ポインタを使って辿ることでアクセスしています。
なおバッハマンは博士号も持っておらず、完全に現場叩き上げの人物ですが、後にこれらの功績でチューリング賞を受賞することとなります。

それに続き、IBMはアポロ計画の部品管理という具体的な課題を解くため、IMS（Information Management System）を1968年に正式稼働させます。
```
**意図:** データバンク構想（変更020）の削除に伴い、ファイルアクセス方式→DBMSという流れで繋ぐために冒頭を差し替え。バッハマン/IDSは史上初のDBMSという具体的な事実なので、抽象的な課題提起より先に置くことで「次はDBMSの登場」という軸が明確になる。

---

### 変更 025
**種類:** DELETE
**before:**
```
一方で階層型の構造は、柔軟なデータの関係を表現するには限界がありました。現実のデータは必ずしも木構造に収まらないからです。たとえば「ある学生が複数の講義を受け、ある講義には複数の学生が所属する」というような、複数の親を持つ関係は階層型では自然に表現できません。この問題を解決しようとしたのがネットワーク型データベースであり、CODASYL委員会を中心として標準化が進められていきます。
```
**after:**
```
一方で階層型の構造は、柔軟なデータの関係を表現するには限界がありました。現実のデータは必ずしも木構造に収まらないからです。たとえば「ある学生が複数の講義を受け、ある講義には複数の学生が所属する」というような、複数の親を持つ関係は階層型では自然に表現できません。
```
（CODASYL言及が削除された）
**意図:** 階層型DBを扱う段落の中にネットワーク型DBの話（CODASYL）が混入していた。扱う題材ごとに文章をまとめる必要があるため削除。

---

### 変更 026
**種類:** ADD
**before:** （コッド論文のタイトルが日本語のみ）
**after:** `「A Relational Model of Data for Large Shared Data Banks（大規模共有データバンクのためのリレーショナルデータモデル）」`
（英題を追記）
**意図:** 論文は原文（英語）で引用する方針。日本語訳は括弧内に補足として残す。

---

### 変更 027
**種類:** DELETE（見出しの統合）
**before:**
```
#### IBMが動かなかった理由

しかし、画期的な理論が生まれたからといって、すぐに製品が世に出るわけではありません。リレーショナルデータベースの実用化には、意外にも時間がかかりました。
```
**after:** 見出しが削除され、内容は `#### リレーショナルモデルの誕生` 節末尾に段落として統合。冒頭文が `しかし、リレーショナルデータベースの実用化には、意外にも時間がかかりました。` に短縮。
**意図:** 内容が薄く、独立した節として立てる強度がなかったため、前の節に統合。節を立てるには一定の内容量・独立性が必要。

---

### 変更 028
**種類:** ADD（第二節の導入セクション追加）
**before:** `## 第二節：Embedded SQLの仕組み` の直後に即、3フェーズ説明の散文
**after:**
```
### Embedded SQLの概要
ではここからEmbedded SQLについての解説を始めます。
Embedded SQL（埋め込みSQL）とはその名の通り、プログラミングソースの中にSQLを直接埋め込む手法です。主にCOBOL、C、Fortran、Adaなどの言語向けに提供されていました。

Embedded SQLのはざっくりと以下のような仕組みでアプリケーションとデータベースを協動させていました。

- SQLのソースコード埋め込み
- プレコンパイルによるSQLの変換
- 実行

このプレコンパイルによるSQLの変換というのがEmbedded SQLの一番の特徴です。...
```
**意図:** Embedded SQLは読者がほぼ知らない技術なので、詳細に入る前に「こんな技術です」という概要を先に提示して読み手の負担を下げた。読みやすさが最優先。

---

### 変更 029
**種類:** REWRITE（節タイトル変更）
**before:** `### COBOLの中にSQLを書く`
**after:** `### ソースコード埋め込み`
**意図:** 見出しは「何について書くか」をシンプルに事実として伝えるもの。「COBOLの中にSQLを書く」は意味がとりにくく、読み手の認知リソースを無駄に消費する。見出しで目を引こうとするより、内容を端的に示すほうが優先。

---

### 変更 030
**種類:** ADD（コードブロックへのCOBOLコメント追加）
**before:**
```cobol
    EXEC SQL
        SELECT EMP_NAME
        INTO   :WS-EMPNAME
        FROM   EMPLOYEE
        WHERE  EMP_ID = :WS-EMPID
    END-EXEC.
```
**after:**
```cobol
    EXEC SQL *> 開始キーワード *
        SELECT EMP_NAME
        INTO   :WS-EMPNAME *> 取得結果を変数に代入している *
        FROM   EMPLOYEE
        WHERE  EMP_ID = :WS-EMPID
    END-EXEC. *> 終了キーワード *
```
（コードブロック直後に注記 `（`*>  *`はCOBOLのコメントアウト記法です）` も追加）
**意図:** 読みやすさのため。コードよりも日本語の方が一瞬で理解できるため、重要な部分にインラインコメントで日本語を添える。

---

### 変更 031
**種類:** REWRITE（コード後の説明段落を簡潔化）
**before:**
```
こうして見ると実にシンプルな仕組みのように思えますが、ここに根本的な問題が一つありました。COBOLのコンパイラは、`EXEC SQL` と `END-EXEC` の間に書かれた文をまったく理解できないのです。COBOLはCOBOLのための言語であって、SQLを解釈する機能など持ち合わせていません。SQLを含んだCOBOLのソースファイルをそのままコンパイラに渡せば、未知の構文があるとしてエラーになるだけです。

この問題を解決するために設けられたのが、プレコンパイルという工程でした。
```
**after:**
```
このようにシンプルな書き方で、SQLを書くことができました。ただ、COBOLのコンパイラは、`EXEC SQL` と `END-EXEC` の間に書かれた文をまったく理解できませんので、SQLを含んだCOBOLのソースファイルをそのままコンパイラに渡せば、未知の構文があるとしてエラーになります。
この問題を解決するために設けられたのが、次に続くプレコンパイルという工程です。
```
**意図:** わかりにくく意味のない文章（「こうして見ると実にシンプルな仕組みのように思えますが」など）を削除し、端的に書き直した。

---

### 変更 032
**種類:** REWRITE（節タイトル変更とサブセクション化）
**before:** `### プレコンパイル：COBOL側の処理`（一節でCOBOL側とDB2側をすべてカバー）
**after:**
```
### プレコンパイルによるSQLの変換
（前置きリスト追加）
#### COBOL側のSQL文置き換え
...
#### DBRMの生成とDB2への受け渡し
...
#### DB2側でカタログ登録
...
```
**意図:** 冒頭で「3つのフェーズ」と宣言している以上、構成もそれに揃えないと読み手が混乱する。プレコンパイルはAIが6節に分けていたが、そのうちCOBOL側・DBRM・DB2側はまとめてプレコンパイルの一節として扱える。タイトルも「COBOL側の処理」だけでは内容が狭すぎたため、フェーズ全体を示す名前に変更。

---

### 変更 033
**種類:** ADD（DBRMのツリー構造図の追加）
**before:** DBRMの内容を散文で説明
**after:** 散文の前に以下のツリー図を追加
```
DBRM
├── ヘッダ情報
│   ├── プログラム名
│   ├── プリコンパイル日時
│   └── DBMSのバージョン情報
├── SQL文のテキスト（各SQL文ごと）
├── ホスト変数の情報
│   ├── 変数名
│   ├── データ型
│   └── 長さ
└── セクション番号（各SQL文の識別子）
```
**意図:** 文字だけでは構造が理解しにくいため、ツリー図で視覚化した。

---

### 変更 034
**種類:** DELETE
**before:**
```
Consistency Tokenは、分離されたCOBOL側のコードとSQL側のデータが、実行時に正しく対応していることを保証するための仕掛けです。プリコンパイル時に同じタイムスタンプがCOBOL側とDBRM側の両方に刻まれ、実行時に照合されます。
```
**after:** （削除）
**意図:** バージョン差異などにより根拠が薄く、事実誤認を書くリスクがあった。流れにとっても不要な情報なので、不確かな情報を残すより削除を選んだ。

---

### 変更 035
**種類:** REWRITE（節タイトル変更と構成変更）
**before:** `### ランタイムの舞台裏：3つの仕掛け`
説明が3コンポーネントをそれぞれ散文で説明（順番に段落が連なる）
**after:** `### 実行`
- 冒頭に3コンポーネントのリストを追加
- `（DSNはDB2の開発時コードネームです。）` を注記として追加
- DSNHADDRの説明に `（DSN Host ADDRess）` を追記
- DSNHLIの説明に `（DSN High Level Interface）` を追記
- 「静かな橋渡し役です」の比喩的記述が削除
**意図:** コンポーネント名（DSNHADDR等）が極端に理解しにくいため、冒頭リストで「3つのものがある」と先に提示する。詳細を読む段階では既に名前に触れているので、初見の難解な名前と詳細説明を同時に処理する認知負荷を下げられる。タイトルはシンプルに事実を示す形に変更（変更029と同じ方針）。

---

### 変更 036
**種類:** REWRITE（表現の変更）
**before:** `プレコンパイラが自動的に挿入したコードが内部でひっそりと動くという設計でした。`
**after:** `プレコンパイラが自動的に挿入したコードが内部で勝手に動くという設計でした。`
**意図:** 「ひっそり」という語感が実態に合わなかった。語の選択は感覚的な判断が入る領域。

---

### 変更 037
**種類:** REWRITE（節タイトル変更とサブセクション化）
**before:** `### プレコンパイル方式の光と影`
**after:**
```
### プレコンパイル方式の特性

Embedded SQLのプレコンパイル方式は、RDBとホスト言語を繋ぐ実用的な解法として広く採用されました。ここではそのメリットとデメリットを整理しましょう。

#### メリット
...
#### デメリット
...
```
**意図:** 「光と影」は意味のない比喩表現。比喩は難しい内容を伝えるために使うものであり、「メリット・デメリット」とシンプルに言い換えられる内容に使うと認知コストだけが上がる。メリット・デメリットの見出しを明示することで構造もわかりやすくなった。

---

### 変更 038
**種類:** REWRITE（見出しレベル変更）
**before:** `### 第一章のまとめ：橋渡し技術としてのEmbedded SQL`
**after:** `## 第一章のまとめ：橋渡し技術としてのEmbedded SQL`
**意図:** 章のまとめは章（`#`）の一つ下のレベル（`##`）に置くべき。節（`###`）レベルに置くのは階層的に誤り。
