---
title: "TiDB Cloud 相乗り環境のコネクションプール枯渇問題"
emoji: "🔖"
type: "tech"
topics: ["tidb", "database", "connectionpool", "ecs"]
published: false
publication_name: levtech
---

こんにちは。レバテック開発部のもりたです。

弊社では、かつてマイクロサービスとして分散させていた複数システムのデータベースを、TiDB Cloud のクラスターに相乗りさせて運用しています。ECS 上でアプリケーションを動かしており、負荷に応じてタスクがスケールアウトする構成です。

この構成を運用していると、コネクションプールの枯渇が潜在的なリスクになりえます。本記事では、なぜそうなるのかと、どう対処できるかを整理します。

## コネクションプールのきほん

コネクションプールは、アプリケーション側がデータベースへの接続をあらかじめ確保しておき、リクエストのたびに使い回す仕組みです。接続の確立はそれなりにコストがかかる処理なので、プールしておくことでオーバーヘッドを減らしています。

プールの設定はアプリケーションの起動時に行います。主に「最大何本のコネクションを持つか（`max`）」と「コネクション取得を何秒待つか（タイムアウト）」を指定します。この設定は ORM やデータベースライブラリごとに書き方が異なります。[^orm-pool-settings]

[^orm-pool-settings]: 代表的な ORM の設定方式は以下のとおりです。大きく3パターンに分類できます。**URLクエリパラメータ方式**（Prisma v6 以前：`?connection_limit=10&pool_timeout=10`）、**設定オブジェクト方式**（Knex.js・SQLAlchemy・TypeORM：初期化時にオブジェクトで渡す）、**メソッドチェーン方式**（GORM：接続後に `db.DB()` 経由で `SetMaxOpenConns()` 等を呼ぶ）。

## どんな問題が起きるか

### コネクションプールの枯渇

データベース側には、同時に受け付けられる接続数の上限があります。アプリケーション側がプールとして確保しようとするコネクション数の合計がこの上限に達すると、新たな接続が拒否されエラーになります。

相乗り環境とECSスケールアウトはこの問題と特に相性が悪いです。

### 相乗り環境との相性

相乗り環境では、複数のアプリケーションが同じデータベースクラスターに接続します。各アプリが `max=10` のプールを持っているとして、アプリが50本あれば単純計算で最大500コネクションを要求することになります。

### ECSスケールアウトとの相性

ECSではタスクが増えるたびに、そのタスクが新たにプールを張ります。1タスクあたり `max=10` であれば、タスクが10に増えると100コネクション、20に増えると200コネクションが必要になります。

相乗り環境にECSのスケールアウトが重なると、「アプリ数 × タスク数 × プール上限」という積が一気に膨らみ、データベースの接続上限に触れやすくなります。

## こう対応する

対策は大きく2種類あります。

### ① アプリケーション側でコネクション数を管理する

データベースの接続上限を超えないよう、各アプリのプール設定を調整する方法です。おおよそ次の流れで進めます。

1. **データベース側の上限を確認する**：TiDB Cloud であればコンソールやドキュメントで最大接続数を確認します
2. **アプリケーションごとの使用量を見積もる**：アプリ数・タスク最大数・1タスクあたりのプール上限から、合計コネクション数を計算します
3. **各アプリのプール上限を調整する**：合計が上限を超えないように `max` の値を設定します

計算式の目安は次のとおりです。

```
1タスクあたりの max ≦ DB接続上限 ÷ アプリ数 ÷ タスク最大数
```

シンプルですが、アプリや環境が増えるたびに再計算が必要になるため、管理コストが上がりやすいというデメリットがあります。

### ② コネクションプーリングプロキシを導入する

アプリケーションとデータベースの間にプロキシを挟み、プロキシ側でコネクションを一元管理する方法です。各アプリはプロキシに接続し、プロキシがデータベースへの接続をまとめて管理します。

アプリが増えても設定を個別に変更する必要がなく、スケールアウト時の接続数増加も吸収しやすいのが利点です。ただし、プロキシ自体の運用が増えるというトレードオフがあります。

## まとめ

相乗り環境 × ECS のスケールアウトという構成では、コネクション数が予想以上に積み上がりやすいため、あらかじめ上限を意識した設計が必要です。

なお、コネクション数を増やしすぎるとデータベース側の負荷も上がります。接続を確立・維持すること自体がDBのリソースを消費するため、「上限に達していないから安全」ではなく、適切な範囲に収めることを意識してください。

ありがとうございました。
